name: Test ZAP AF - Command Options Input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create a minimal AF plan file
        run: |
          cat > "$GITHUB_WORKSPACE/af-plan.yaml" << 'EOF'
          env:
            contexts:
              - name: "Test Context"
                urls:
                  - "http://localhost"
          jobs:
            - type: passiveScan-config
              parameters:
                maxAlertsPerRule: 10
          EOF

      - name: Run ZAP AF action with cmd_options (expected to fail - alpine has no zap.sh)
        id: zap_action
        uses: .
        with:
          plan: af-plan.yaml
          docker_name: alpine:3.18
          cmd_options: '-config api.disablekey=true'
        continue-on-error: true

      - name: Assert action ran with cmd_options (failed as expected with non-ZAP image)
        run: |
          echo "outcome: ${{ steps.zap_action.outcome }}"
          # The action should fail because alpine does not have zap.sh
          # This confirms the action correctly processed cmd_options input
          # and attempted to run Docker with those options appended
          if [ "${{ steps.zap_action.outcome }}" = "failure" ]; then
            echo "PASS: action correctly failed when zap.sh is not available, confirming cmd_options were processed"
          else
            echo "FAIL: expected action to fail with non-ZAP image, but got: ${{ steps.zap_action.outcome }}"
            exit 1
          fi
